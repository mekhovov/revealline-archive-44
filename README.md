# RevealLine archive 44

Preserves the original v0.79.1 distribution without rebuilding historical source.
Source `9876f995bc155638cf9dcc62b5d6e1daec963b27` / tree `aae498df146ed7dccb1be0f15258fa3717ddf0a5`.

Expected inventory and byte totals are generated from the immutable v0.79.1 manifest plus the archive support files and are enforced beneath the unchanged 800,000,000-byte budget. This new archive has zero prior accepted paths. Its public byte and native admission remain separate post-deployment gates.

The original v0.79.1 release qualification covers its exact source and scoped native journeys. It does not establish whole-phase, physical controller/touch, or audible-listening acceptance. Main public verification covers all 4,260 files and scoped browser journeys; physical iPhone, Steam Deck and controller qualification remain open.

Only main deploys through GitHub Actions. The original metadata and qualification are copied byte for byte; the pinned extractor and unchanged prepare/verify helpers check every output body. Local checks are not public acceptance.
